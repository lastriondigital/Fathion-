import { DEFAULT_BIBLE_VERSIONS, bibleVersionRegistry, BIBLE_VERSIONS } from '../data/bibleVersions';
import { BibleVersion } from '../types';

export { DEFAULT_BIBLE_VERSIONS, bibleVersionRegistry, BIBLE_VERSIONS };
export type { BibleVersion };
